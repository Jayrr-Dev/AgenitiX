/**
 * MIGRATION: CreateText v1 -> v2
 *
 * Placeholder script. When you bump createText.version to 2, fill in the
 * migrate() function to transform saved node data.
 */

export const migrate = (oldData: any): any => {
  // TODO: implement when version 2 ships
  return { ...oldData };
};
