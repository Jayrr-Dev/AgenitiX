/**
 * DEFAULTS INDEX - Default configuration exports
 *
 * • Exports default configurations and utilities
 * • Provides clean import paths for default values
 * • Includes node defaults and configuration helpers
 *
 * Keywords: defaults, exports, configuration
 */

// Export node defaults (when fixed)
// export * from "./nodeDefaults";

// For now, export basic defaults
export const DEFAULT_PROCESSING_THROTTLE = 100;
export const DEFAULT_GPU_THRESHOLD = 50;
export const DEFAULT_IDLE_TIMEOUT = 3000;
