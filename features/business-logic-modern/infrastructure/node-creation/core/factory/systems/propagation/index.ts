/**
 * PROPAGATION SYSTEM - Ultra-fast node activation propagation
 *
 * • Exports high-performance propagation engine and utilities
 * • Provides GPU-accelerated visual updates and state transitions
 * • Implements enterprise-grade error handling and monitoring
 * • Supports real-time node activation with state machine logic
 *
 * Keywords: propagation, ultra-fast, gpu-acceleration, state-machine, enterprise
 */

export {
  UltraFastPropagationEngine,
  useUltraFastPropagation,
} from "./UltraFastPropagationEngine";

export type {
  NodeState,
  PropagationEngineError,
  TransitionEvent,
} from "./UltraFastPropagationEngine";
