# Example Environment Variables for Production

# ================================
# REQUIRED FOR PRODUCTION
# ================================

# Resend Email Service (Required for production emails)
RESEND_API_KEY=re_your_resend_api_key_here

# Your production domain (Required for magic links)
NEXT_PUBLIC_APP_URL=https://yourdomain.com

# ================================
# CONVEX DATABASE (Already configured)
# ================================

# Convex deployment URL (should already be set)
CONVEX_DEPLOYMENT=your_convex_deployment_name
NEXT_PUBLIC_CONVEX_URL=https://your_convex_url

# ================================
# OPTIONAL CONFIGURATION
# ================================

# Custom email domain (if you have domain verification in Resend)
RESEND_FROM_EMAIL=noreply@yourdomain.com

# Node environment (automatically set by hosting platforms)
NODE_ENV=production

# ================================
# INSTRUCTIONS
# ================================

# 1. Copy these variables to your hosting platform's environment settings
# 2. Replace the placeholder values with your actual values
# 3. Never commit actual API keys to version control

# For Vercel: Add these in Vercel dashboard → Settings → Environment Variables
# For Netlify: Add these in Netlify dashboard → Site settings → Environment variables
# For Railway: Add these in Railway dashboard → Variables