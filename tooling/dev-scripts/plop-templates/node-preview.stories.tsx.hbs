/**
 * NODE PREVIEW STORY TEMPLATE - Auto-generated by Plop
 *
 * • Wraps the node in ReactFlowProvider for context
 * • Uses the node's spec.initialData for realistic default state
 * • Follows project story conventions for layout and naming
 *
 * Keywords: storybook, node-preview, plop-template
 */

import { ReactFlowProvider } from "@xyflow/react";

// Import the node component and its spec
import {{pascalCase kind}}, { spec as {{pascalCase kind}}Spec } from "../features/business-logic-modern/node-domain/{{domain}}/{{kind}}.node";

export default {
  title: "Nodes/{{pascalCase kind}}/Preview",
  parameters: { layout: "fullscreen" },
};

export const Default = () => (
  <ReactFlowProvider>
    <{{pascalCase kind}} data={ {{pascalCase kind}}Spec.initialData } id="storybook-{{kebabCase kind}}" />
  </ReactFlowProvider>
);
